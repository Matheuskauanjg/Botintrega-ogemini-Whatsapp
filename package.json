{
  "name": "whatsapp-personal-render-launcher",
  "version": "1.0.0",
  "private": true,
  "description": "Launcher da bridge pessoal WhatsApp para Render",
  "scripts": {
    "postinstall": "npm --prefix whatsapp-personal-render install --omit=dev",
    "start": "npm --prefix whatsapp-personal-render start"
  },
  "engines": {
    "node": ">=20"
  }
}
